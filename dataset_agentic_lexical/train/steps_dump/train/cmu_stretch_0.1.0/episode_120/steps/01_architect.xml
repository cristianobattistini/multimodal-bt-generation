<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence>
      <!-- Move to green_garbage_can_lid -->
      <Action ID="MOVE_TO" obj="green_garbage_can_lid" />
      <!-- Pick green_garbage_can_lid -->
      <Action ID="PICK" obj="green_garbage_can_lid" />
      <!-- Unlock green_garbage_can_lid -->
      <Action ID="UNLOCK" obj="green_garbage_can_lid" />
      <!-- Let go -->
      <Action ID="LET_GO" />
    </Sequence>
  </BehaviorTree>
</root>
