scene_analysis:
  target: "blue_pen"
  destination: ""
  expanded_instruction: "First check IS_REACHABLE on the blue_pen and retry the check up to 2 times (within a 10s timeout); then reach and grasp the blue pen in the pen holder on the table to the right of the stack, avoiding the stack obstacle. If grasp fails, push to reposition and retry once."
  scene_context: "Blue pen upright in a cup on the table right of a stack_of_boxes; robot arm left of stack; table clear; nothing held."
  expected_sequence: "Check IS_REACHABLE(blue_pen) before NAVIGATE_TO, retry up to 2 times if needed; Move around the stack_of_boxes to the cup, extend the arm into the holder, and grasp the blue pen. If grasp fails, push to reposition and retry once."
