scene_analysis:
  target: "grid_clamp"
  destination: "grid_board"
  expanded_instruction: "Using the ApproachGridClamp subtree, approach and pick up the grid_clamp (retry up to 3 times, 10s timeout; if grasp fails, push to reposition) and place it into an empty slot on the grid_board"
  scene_context: "grid_clamp on the front table; grid_board laid out on work surface; clamp not held; surface clear"
  expected_sequence: "Use the ApproachGridClamp subtree to pick up the grid_clamp from the table (retry up to 3 times with 10s timeout; if grasp fails, push to reposition) and place it into an empty slot on the grid_board"
