scene_analysis:
  target: "dishes"
  destination: "dish_rack"
  expanded_instruction: "Use the ApproachYellowPlate subtree: pick up the yellow plate from the drying towel (retry up to 3 times, 10s timeout; if grasp fails, push to reposition) and place it upright into the dish_rack."
  scene_context: "Dish_rack empty and accessible; yellow plate lying on towel next to rack; white cup on towel upright."
  expected_sequence: "Use the ApproachYellowPlate subtree: reach to the plate on the towel (retry up to 3 times, 10s timeout; if grasp fails, push to reposition), lift it, move over the dish_rack, and place it upright into a slot."
