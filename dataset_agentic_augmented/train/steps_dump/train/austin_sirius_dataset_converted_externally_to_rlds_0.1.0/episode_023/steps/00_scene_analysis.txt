scene_analysis:
  target: "["blue_gear", "red_gear"]"
  destination: "right_peg"
  expanded_instruction: "Group the actions into an ApproachBlueGear subtree: pick up the blue gear from the table and insert it onto the right peg (if grasp fails, retry up to 2 times), then pick up the red gear and place it onto the same right peg."
  scene_context: "Blue and red gears on the table; right peg empty and reachable; no gripper hold; work surface clear."
  expected_sequence: "ApproachBlueGear subtree: pick and place the blue gear onto the right peg (grasp retried up to 2 times if it fails), then pick and place the red gear onto the same peg."
