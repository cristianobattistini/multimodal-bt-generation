scene_analysis:
  target: "pot"
  destination: "sink"
  expanded_instruction: "First check the sink is visible; if not, navigate to the sink (navigate action should complete within a 15s timeout). Grasp the metal pot lying on its side in the sink and flip it upright inside the sink."
  scene_context: "Metal pot is on its side inside the sink; sink is open and not occupied; gripper not holding the pot."
  expected_sequence: "Check IS_VISIBLE on sink, NAVIGATE_TO sink (within 15s timeout) if not visible, reach to the pot, grasp it, rotate it upright, and set it down upright in the sink."
