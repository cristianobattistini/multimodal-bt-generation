scene_analysis:
  target: "apple"
  destination: "top_drawer"
  expanded_instruction: "Pick up the apple from the countertop and place it into the top drawer, using NAVIGATE_TO with a 15s timeout when moving to the apple."
  scene_context: "Apple on countertop; top drawer is open and empty; apple not held; path into drawer clear."
  expected_sequence: "Move to the apple using NAVIGATE_TO with a 15s timeout, grasp it, move over the open top drawer, and set the apple inside."
