scene_analysis:
  target: "white_box"
  destination: ""
  expanded_instruction: "Pick up the white box from the table, grouping the approach/grasp/lift steps into an ApproachWhiteBox subtree; if the grasp fails, retry up to 2 times."
  scene_context: "White box on the right side of the table, not held; table surface reachable and no container present."
  expected_sequence: "Approach the white box (as part of the ApproachWhiteBox subtree), grasp it securely—if grasp fails, retry up to 2 times—and lift it off the table."
