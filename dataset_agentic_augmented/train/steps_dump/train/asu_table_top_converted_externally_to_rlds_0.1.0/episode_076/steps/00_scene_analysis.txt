scene_analysis:
  target: "white_box"
  destination: ""
  expanded_instruction: "Pick up the white box from the table and lift it clear of the surface; if a grasp fails, retry up to 3 times (within a 10s timeout) and if still unsuccessful, push or flip the box to reposition it before grasping."
  scene_context: "White box on table near front-left, table clear, box not held, robot arm nearby."
  expected_sequence: "Move to the white box, grasp it securely, and lift it off the table; if grasping fails, retry (up to 3 times) and then use push or flip to reposition the box and attempt the grasp again."
