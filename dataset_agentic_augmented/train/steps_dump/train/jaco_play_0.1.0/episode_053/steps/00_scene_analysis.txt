scene_analysis:
  target: "gray_bowl"
  destination: "sink"
  expanded_instruction: "Check that the gray bowl is IS_GRASPABLE before grasping (retry up to 3 times or reposition if not), then pick up the gray bowl from the rack/platform near the sink and place it into the sink basin"
  scene_context: "Gray bowl resting on a rack/platform to the right of the sink; sink basin (blue) is open and clear; bowl is not held."
  expected_sequence: "Move to the gray bowl, verify IS_GRASPABLE before attempting to grasp (retry up to 3 times or reposition if grasp fails), grasp it, carry it over the sink, and set it down inside the sink."
