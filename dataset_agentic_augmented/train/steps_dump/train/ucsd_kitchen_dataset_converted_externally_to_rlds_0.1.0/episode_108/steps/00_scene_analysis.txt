scene_analysis:
  target: "carbinet_door"
  destination: ""
  expanded_instruction: "Open the carbinet door of the lower cabinet by grasping its handle and pulling it open. If grasp fails, retry up to 3 times within a 10s timeout; if still unsuccessful, use a push or flip action to reposition the handle and then pull."
  scene_context: "Lower cabinet door is closed and latched with visible handle; robot not holding the handle."
  expected_sequence: "Move to the handle, grasp it, and pull the door until it swings open. If grasp fails after up to 3 attempts (within 10s), push or flip to reposition the handle and then pull."
