scene_analysis:
  target: "toast"
  destination: "toaster_slot"
  expanded_instruction: "Pick up the slice of toast and insert it into the toaster slot; if a grasp fails, retry up to 3 times and, if still unsuccessful, push or flip the toast to reposition before trying again (within a 10s timeout)."
  scene_context: "Toaster upright on surface; single slice of toast beside it and not held; slot accessible."
  expected_sequence: "Reach the toast, attempt to pick it up and place it into the toaster slot; if grasp fails, retry up to 3 times, then use push or flip to reposition and try again within 10s."
