scene_analysis:
  target: "grape"
  destination: ""
  expanded_instruction: "Pick up a single grape from the bunch on the plate; if a grasp fails, retry up to 2 times, each grasp attempt within a 15s timeout."
  scene_context: "Grape bunch on a white plate on the table; plate and table clear; robot not holding anything."
  expected_sequence: "Approach the plate, grasp one grape from the bunch (each grasp attempt within a 15s timeout; retry up to 2 times if it fails), and lift it clear of the plate."
