scene_analysis:
  target: "radio_receiver"
  destination: ""
  expanded_instruction: "Go to the radio receiver (radio on the table near the front), pick up the radio, and turn it on (set to ON)."
  scene_context: "Radio on table near front; radio_receiver not held; floor and table clear; no containers open."
  expected_sequence: "Move to the radio, grasp it (if held, release and reposition), then turn it ON by switching to the ON position."
Plan:
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence>
      <!-- Navigate to radio_receiver -->
      <Action ID="NAVIGATE_TO" obj="radio_receiver" />
      <!-- Toggle on radio_receiver -->
      <Action ID="TOGGLE_ON" obj="radio_receiver" />
    </Sequence>
  </BehaviorTree>
</root>