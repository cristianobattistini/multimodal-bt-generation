scene_analysis:
  target: "radio_receiver"
  destination: ""
  expanded_instruction: "Pick up the radio receiver from the table and turn it on by connecting its antenna to the radio receiver."
  scene_context: "Radio is on the table near the front; robot not holding it; table surface clear."
  expected_sequence: "Move to the radio, pick it up, align the antenna with the receiver, and turn it on."
Plan:
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence>
      <!-- Navigate to radio_receiver -->
      <Action ID="NAVIGATE_TO" obj="radio_receiver" />
      <!-- Toggle on radio_receiver -->
      <Action ID="TOGGLE_ON" obj="radio_receiver" />
    </Sequence>
  </BehaviorTree>
</root>