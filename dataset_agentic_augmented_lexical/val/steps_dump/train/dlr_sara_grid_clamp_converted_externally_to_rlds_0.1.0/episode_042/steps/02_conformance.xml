<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <Sequence>
      <!-- Navigate to grid_clamp -->
      <Action ID="NAVIGATE_TO" obj="grid_clamp" />
      <!-- Grasp grid_clamp -->
      <Action ID="GRASP" obj="grid_clamp" />
      <Sequence>
        <!-- Check grid is visible before NAVIGATE_TO -->
        <Condition ID="IS_VISIBLE" obj="grid" />
        <!-- Navigate to grid -->
        <Action ID="NAVIGATE_TO" obj="grid" />
      </Sequence>
      <!-- Place on grid -->
      <Action ID="PLACE_ON_TOP" obj="grid" />
    </Sequence>
  </BehaviorTree>
</root>
