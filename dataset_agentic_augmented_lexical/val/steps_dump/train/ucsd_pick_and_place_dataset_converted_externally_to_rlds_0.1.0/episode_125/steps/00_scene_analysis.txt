scene_analysis:
  target: "pot"
  destination: "sink"
  expanded_instruction: "Pick up the pot from the stovetop and place it into the sink. If a grasp fails, retry up to 3 times; if still unsuccessful, use a push or flip to reposition the pot and then pick it up."
  scene_context: "Pot on stovetop; sink is empty and reachable; pot not held."
  expected_sequence: "Move to the pot, lift it, carry it over the sink, and place it inside. If a grasp fails, retry (up to 3 times) or use push/flip to reposition before lifting and placing."
