scene_analysis:
  target: "red_object"
  destination: ""
  expanded_instruction: "Pick up the red object (small red cube) from the table in front of the robot; if a grasp fails, retry up to 3 times and, within a 10s timeout, use pushing or flipping to reposition the cube as a fallback."
  scene_context: "Red cube on table center; robot gripper above and not holding; table unobstructed."
  expected_sequence: "Lower the gripper to the red cube, grasp it, and lift it off the table; if grasp fails, retry (up to 3 attempts) and use push or flip to reposition the object before retrying, all within a 10s timeout."
