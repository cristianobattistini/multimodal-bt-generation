scene_analysis:
  target: "red_object"
  destination: ""
  expanded_instruction: "Pick up the red object (small red rectangular block) from the table center front, attempting a grasp with a 10s timeout and retrying up to 2 times; if grasp fails, push or flip the object to reposition before retrying."
  scene_context: "Table is clear; red_object sits on the table center-front and is not held; robot arm is nearby."
  expected_sequence: "Approach the red object, attempt a grasp (10s timeout) retrying up to 2 times if needed; if grasp fails, push or flip to reposition, then grasp securely and lift it off the table."
