scene_analysis:
  target: "["pot", "lid", "tool"]"
  destination: "plate"
  expanded_instruction: "Remove the lid from the pot, place the pot on the round plate (if placing fails, retry up to 2 times), then use the tool to push the pot to the front edge of the table."
  scene_context: "Pot on stovetop with lid on, round plate reachable nearby, tool lying on table, front table edge clear."
  expected_sequence: "Take off the lid and set it aside, move the pot onto the plate (retry placing up to 2 times if it fails), then use the tool to push the pot forward to the table front."
