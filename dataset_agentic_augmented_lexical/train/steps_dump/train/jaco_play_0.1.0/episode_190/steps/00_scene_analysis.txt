scene_analysis:
  target: "long_bread"
  destination: "sink"
  expanded_instruction: "Pick up the long bread from the table near the robot and place it into the blue sink, navigating to the table with NAVIGATE_TO within a 20s timeout."
  scene_context: "Long_bread on table near robot base, sink (blue) is empty and reachable, nothing is currently held."
  expected_sequence: "Approach and grasp the long_bread (navigate to the table with NAVIGATE_TO within a 20s timeout), carry it over to the sink, and set it down inside the sink."
