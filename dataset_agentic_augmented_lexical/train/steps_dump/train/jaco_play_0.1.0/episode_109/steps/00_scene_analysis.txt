scene_analysis:
  target: "butter_dairy"
  destination: "sink"
  expanded_instruction: "Pick up the yellow butter_dairy from the countertop and place it inside the white sink; if a grasp fails, retry up to 3 times, then use push or flip to reposition the object and try again (each attempt within a 10s timeout)."
  scene_context: "Yellow butter_dairy on countertop near robot gripper; white circular sink is empty and reachable; gripper not holding it."
  expected_sequence: "Approach the butter_dairy, grasp it, lift and move over the sink, and place it inside; if grasp fails, retry up to 3 times, then push or flip to reposition and repeat (each attempt within a 10s timeout)."
