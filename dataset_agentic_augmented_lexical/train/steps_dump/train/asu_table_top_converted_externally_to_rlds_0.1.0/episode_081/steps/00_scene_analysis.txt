scene_analysis:
  target: "white_box"
  destination: ""
  expanded_instruction: "Pick up the white box from the table and hold it in the robot gripper; if a grasp fails, retry up to 3 times within a 10s timeout, then use a push or flip to reposition the object before trying again."
  scene_context: "Robot not holding object; white box on table among other cans and bottles; table is clear enough to reach."
  expected_sequence: "Approach the white box, grasp it, lift it into the gripper, and hold it steady; if grasp fails, retry up to 3 times within 10s, then push or flip to reposition the object and attempt the grasp again."
