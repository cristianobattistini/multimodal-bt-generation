scene_analysis:
  target: "bread"
  destination: ""
  expanded_instruction: "Pick up the bread loaf from the table; if the grasp fails, retry up to 2 times, then lift it upward."
  scene_context: "Bread loaf on left side of table; table clear; robot not holding anything; no closed containers."
  expected_sequence: "Move to the bread, grasp it securely (retry up to 2 times on failure), and lift it upward."
