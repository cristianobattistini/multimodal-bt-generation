scene_analysis:
  target: "bottle"
  destination: ""
  expanded_instruction: "Open the bottle by removing its cap from the blue surface, using an ApproachBottle subtree to approach the bottle; retry up to 3 times with a 10s timeout if needed."
  scene_context: "Bottle on blue fabric with cap on, bottle not held, surface clear."
  expected_sequence: "Approach the bottle (ApproachBottle subtree: retry up to 3 times, 10s timeout), reach and grasp it (if grasp fails, push to reposition), then twist and remove the cap to open it."
