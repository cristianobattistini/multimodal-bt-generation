scene_analysis:
  target: "can"
  destination: ""
  expanded_instruction: "Pick up the small silver can resting on the blue surface; if the grasp fails, retry up to 2 times, allowing a 20s timeout per grasp."
  scene_context: "Small silver can resting on a blue surface near a shadowed edge; surface clear; can not held."
  expected_sequence: "Move to the can, reach down, and grasp it securely; each grasp attempt has a 20s timeout and should be retried up to 2 times if it fails."
