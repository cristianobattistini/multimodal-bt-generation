scene_analysis:
  target: "grid_clamp"
  destination: "grid"
  expanded_instruction: "Pick up the grid clamp from the table and place it onto the grid surface near the front."
  scene_context: "Several grid_clamps lie on the table front; grid surface is clear and reachable; no container is open."
  expected_sequence: "Reach to the clamp, grasp it, move to the grid surface, and place the clamp onto the grid."