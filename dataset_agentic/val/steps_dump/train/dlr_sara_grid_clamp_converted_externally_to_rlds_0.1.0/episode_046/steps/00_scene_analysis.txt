scene_analysis:
  target: "grid_clamp"
  destination: "grid_board"
  expanded_instruction: "Pick up the grid clamp from the table and place it onto the wooden grid board"
  scene_context: "Several grid_clamps are on the table; wooden grid_board is visible and clear; no object is held."
  expected_sequence: "Reach to a grid_clamp on the table, grasp it, move to the grid_board, and place/attach it onto the board."